[package]
name = "jm"
version = "0.1.0"
authors = ["Idan Attias <idana@wix.com>"]
edition = "2018"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

serde_json = "1.0"

[dev-dependencies]
tempdir = "0.3"
